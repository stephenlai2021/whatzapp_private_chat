{
  "name": "whatzapp",
  "version": "0.0.1",
  "scripts": {
    "dev": "svelte-kit dev",
    "build": "svelte-kit build",
    "preview": "svelte-kit preview",
    "push": "git add . && git commit -m commit && git push origin master"
  },
  "devDependencies": {
    "@sveltejs/adapter-vercel": "next",
    "@sveltejs/kit": "next",
    "svelte": "^3.34.0"
  },
  "type": "module",
  "dependencies": {
    "date-fns": "^2.27.0",
    "firebase": "^9.4.1",
    "svelte-emoji-picker": "^1.1.2"
  }
}
